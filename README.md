# EMS

Employee/Enterprise Management System (EMS) built with Flask.

## Quick start

1. Create a virtual environment and install dependencies:
   - `pip install -r requirements.txt`
2. Run the app:
   - `python run_dev.py`

## Notes

- Environment variables are loaded from `.env` (intentionally not committed).

